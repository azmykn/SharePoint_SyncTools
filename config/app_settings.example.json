{
  "_comment": "Copy to config/app_settings.json (gitignored). SharePoint site root is saved from the web UI in SQLite, not here.",
  "database_path": "data/app.sqlite",
  "default_guest_share_link": ""
}
